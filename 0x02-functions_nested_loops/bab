#include "main.h"
/**
 * print_sign - Prints the sugn if a number
 * Return: 1 or 0 or -1
 * @n: Receives a number
 */

int print_sign(int n)
{
	if (n == 0)
	{
		_putchar('0');
		return (0);
	}
	else if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else
}
