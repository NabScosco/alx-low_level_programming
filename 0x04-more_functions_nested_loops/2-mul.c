#include "main.h"

/**
 * mul - Function multiplies two integers.
 * @a: Receives first number.
 * @b: Receives second number.
 * Return: multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
