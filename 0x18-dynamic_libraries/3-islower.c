#include "main.h"
/**
 * _islower - This prints 1 or 0 depending on the inputs.
 * @c: This is the first paarameter
 * Return: Returns always 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
